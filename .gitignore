# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# screenshots locais (só referência visual, não entra no repo)
screenshots/

# mocks de design (PDF/PNGs de referência visual, não entram no repo)
design-refs/

# arquivos-fonte de design (capas YouTube, etc.) — material bruto não
# consumido pelo build, mantido localmente apenas para edição futura
assets/

# PDFs e arquivos brutos de fomento/identidade visual (a régua já está
# extraída em /public/footer/regua-fomento*.{png,webp})
Réguas.pdf
*.brutos.pdf
