---
// =============================================================
// Layout.astro · Shell base de todas as páginas
// Fontes locais carregadas via @font-face em global.css (a partir
// de /public/fonts/): Playfair Display (serif), Inter (sans),
// DIN Alternate Bold e Myriad Pro (detalhes). Não usamos Google Fonts.
// =============================================================
import "../styles/global.css";
import CookieBanner from "../components/CookieBanner.astro";
import Analytics from "@vercel/analytics/astro";

export interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = "(In)finitudes",
  description = "Projeto cultural idealizado por Eleonora C. Santos. Inclui um livro, podcasts, histórias reais, escritas femininas e divulgação de temas relacionados à ciência, à saúde e à espiritualidade.",
  image = "/og-image.png",
} = Astro.props;

// URL canônica (resolve a imagem em URL absoluta para Open Graph)
const canonicalURL = new URL(Astro.url.pathname, Astro.site ?? Astro.url);
const ogImage = new URL(image, Astro.site ?? Astro.url).toString();

// Google Analytics 4 · lê o ID via env (configurado na Vercel, Production only).
// Nunca hardcode o ID; se a variável não existir (ex.: local sem .env), o
// snippet abaixo simplesmente não é renderizado e o build segue normal.
const GA_ID = import.meta.env.PUBLIC_GA_MEASUREMENT_ID;
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />

    {/* Google Analytics 4 · só renderiza quando PUBLIC_GA_MEASUREMENT_ID existe */}
    {
      GA_ID && (
        <>
          <script async src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`} />
          <script is:inline define:vars={{ GA_ID }}>
            window.dataLayer = window.dataLayer || [];
            function gtag() {
              dataLayer.push(arguments);
            }
            gtag("js", new Date());
            gtag("config", GA_ID);
          </script>
        </>
      )
    }

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalURL.toString()} />

    {/* Preload das fontes críticas para evitar FOUT (body + títulos) */}
    <link
      rel="preload"
      href="/fonts/inter-variable.ttf"
      as="font"
      type="font/ttf"
      crossorigin
    />
    <link
      rel="preload"
      href="/fonts/playfair-display-regular.ttf"
      as="font"
      type="font/ttf"
      crossorigin
    />

    <title>{title}</title>

    {/* Open Graph · usado pelo WhatsApp, Facebook, LinkedIn etc. */}
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:site_name" content="(In)finitudes" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL.toString()} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="(In)finitudes — aquarela e logotipo do projeto" />

    {/* Twitter Card */}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
  </head>
  <body class="font-sans bg-paper text-ink">
    <slot />
    <CookieBanner />
    <Analytics />
  </body>
</html>
